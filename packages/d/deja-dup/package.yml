# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : deja-dup
version    : '50.0'
release    : 39
source     :
    - https://gitlab.gnome.org/World/deja-dup/-/archive/50.0/deja-dup-50.0.tar.gz : 7f1efb56aab43b622f9e23acad5c2433b94ca2edab0cce458e4475e199311b42
homepage   : https://apps.gnome.org/DejaDup/
license    : GPL-3.0-or-later
component  : system.utils
summary    : Déjà Dup backup tool
description: |
    Déjà Dup is a simple backup tool. It hides the complexity of backing up the Right Way (encrypted, off-site, and regular) and uses duplicity as the backend.
builddeps  :
    - pkgconfig(blueprint-compiler)
    - pkgconfig(gtk4)
    - pkgconfig(json-glib-1.0)
    - pkgconfig(libadwaita-1)
    - pkgconfig(libsecret-1)
    - pkgconfig(libsoup-3.0)
    - pkgconfig(wayland-client)
    - desktop-file-utils
    - itstool
    - vala
rundeps    :
    - duplicity
    - gvfs
    - rclone
    - restic
setup      : |
    %meson_configure -Denable_restic=true
build      : |
    %ninja_build
install    : |
    %ninja_install
    mv $installdir/etc/xdg $installdir/usr/share/xdg
    rmdir $installdir/etc
